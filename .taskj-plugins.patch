--- a/plugins/tools/actions/src/main.rs
+++ b/plugins/tools/actions/src/main.rs
@@ -9,7 +9,7 @@
 
 use anyhow::{Context, Result};
 use mcp_server_util::*;
-use omniagent::tasks_yaml::{self, PlanningMode, ScheduleDef};
+use omniagent::tasks_yaml::{self, ScheduleDef};
 use parking_lot::Mutex;
 use serde_json::Value;
 use sql_forge::sql_forge;
@@ -293,7 +293,7 @@
     let def = ScheduleDef {
         enabled: true,
         channel: Some("cron-default".to_string()),
         profile: Some("pipeline".to_string()),
-        planning_mode: Some(PlanningMode::Str("plan_with_subtasks".to_string())),
+        plan: Some(true),
         cron: schedule.to_string(),
         prompt: Some(prompt.to_string()),
         action: None,
--- a/plugins/tools/cron/src/main.rs
+++ b/plugins/tools/cron/src/main.rs
@@ -127,7 +127,7 @@
     let def = ScheduleDef {
         enabled: true,
         channel: channel_name,
         profile: profile_arg.map(|s| s.to_string()),
-        planning_mode: None,
+        plan: None,
         cron: schedule.to_string(),
         prompt: Some(prompt.unwrap_or("").to_string()),
         action: if mode == "action" {
