{
  "id": "mcp-server-actions",
  "name": "Actions MCP Server",
  "version": "0.1.0",
  "description": "Built-in action tools \u2014 hindsight_populator, relevance_indexer, setup_knowledge_pipeline",
  "type": "mcp",
  "entrypoint": {
    "command": "mcp-server-actions",
    "args": [],
    "transport": "stdio"
  },
  "config_schema": [
    {
      "key": "HINDSIGHT_URL",
      "label": "Hindsight Service URL",
      "type": "string",
      "required": false
    },
    {
      "key": "database_url",
      "default": "$env:DATABASE_URL",
      "label": "Database Url",
      "type": "string"
    },
    {
      "key": "omni_dir",
      "default": "$env:OMNI_DIR",
      "label": "Omni Dir",
      "type": "string"
    },
    {
      "key": "llm_provider",
      "default": "$env:LLM_PROVIDER",
      "label": "LLM Provider",
      "type": "string",
      "required": false,
      "description": "Default LLM provider used for kanban-dispatched threads when the channel has no provider set."
    },
    {
      "key": "omniagent_url",
      "default": "$env:OMNIAGENT_URL",
      "label": "Omniagent Url",
      "type": "string",
      "required": false,
      "description": "Base URL of the omniagent core HTTP API for POST /kanban/dispatch. Falls back to http://localhost:8080 when unset."
    }
  ],
  "label": "actions"
}
