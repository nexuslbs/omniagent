{
  "id": "mcp-server-filesystem",
  "name": "Filesystem MCP Server",
  "label": "filesystem",
  "version": "0.1.0",
  "description": "Filesystem tools: read, write, list, search, info on local files (writes sandboxed to workspace_dir; reads unrestricted)",
  "type": "mcp",
  "entrypoint": {
    "command": "mcp-server-filesystem",
    "args": [],
    "transport": "stdio"
  },
  "config_schema": [
    {
      "key": "workspace_dir",
      "default": "/opt/workspace",
      "label": "Workspace Dir (write sandbox — file WRITES only allowed here and in subdirectories; reads are unrestricted)",
      "type": "string",
      "required": true
    }
  ]
}