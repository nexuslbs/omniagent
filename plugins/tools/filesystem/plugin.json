{
  "id": "mcp-server-filesystem",
  "name": "Filesystem MCP Server",
  "label": "filesystem",
  "version": "0.1.0",
  "description": "Filesystem tools: read, write, list, search, info on local files",
  "type": "mcp",
  "entrypoint": {
    "command": "mcp-server-filesystem",
    "args": [],
    "transport": "stdio"
  },
  "config_schema": [
    {
      "key": "workspace_dir",
      "default": "/opt/workspace",
      "label": "Workspace Dir (sandbox — all filesystem operations only allowed here and in subdirectories)",
      "type": "string",
      "required": true
    }
  ]
}