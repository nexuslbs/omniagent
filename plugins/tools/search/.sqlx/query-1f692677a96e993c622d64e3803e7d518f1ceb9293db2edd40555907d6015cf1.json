{
  "db_name": "PostgreSQL",
  "query": "        UPDATE threads t\n            SET status = $1,\n                ended_at = NOW(),\n                input_tokens = usage_agg.input_tokens,\n                cached_tokens = usage_agg.cached_tokens,\n                output_tokens = usage_agg.output_tokens,\n                iterations = COALESCE(\n                    (SELECT MAX(iteration_number) FROM messages WHERE thread_id = $2),\n                    0\n                ),\n                terminal = true\n            FROM (\n                SELECT\n                    COALESCE(SUM(COALESCE((m.token_usage->>'prompt_tokens')::bigint, 0)), 0)::int AS input_tokens,\n                    COALESCE(SUM(COALESCE((m.token_usage->>'cached_tokens')::bigint, 0)), 0)::int AS cached_tokens,\n                    COALESCE(SUM(COALESCE((m.token_usage->>'completion_tokens')::bigint, 0)), 0)::int AS output_tokens\n                FROM messages m\n                WHERE m.thread_id = $3 AND m.msg_type <> 'error'\n            ) usage_agg\n            WHERE t.id = $4 AND NOT t.terminal",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Text",
        "Int8",
        "Int8",
        "Int8"
      ]
    },
    "nullable": []
  },
  "hash": "1f692677a96e993c622d64e3803e7d518f1ceb9293db2edd40555907d6015cf1"
}
