{
    "servers": [
        {
            "name": "skills",
            "transport": "stdio",
            "args": [],
            "timeout_secs": 30,
            "max_retries": 2,
            "allowed_tools": [
                "*"
            ],
            "env": {
                "OMNI_DIR": "${OMNI_DIR:-/opt/data}",
                "RUST_LOG": "info"
            }
        }
    ]
}