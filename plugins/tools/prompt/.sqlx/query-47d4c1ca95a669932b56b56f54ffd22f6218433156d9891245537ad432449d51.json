{
  "db_name": "PostgreSQL",
  "query": "\n        INSERT INTO kanban_history (kanban_task_id, action, initial_board, final_board, previous_values)\n        SELECT\n            surv.id,\n            'dependency_removed', NULL, NULL,\n            jsonb_build_object(\n                'depends_on_id', del.id,\n                'title', COALESCE(NULLIF(del.title, ''), '(deleted task)')\n            )\n        FROM kanban_task_dependencies d\n        JOIN kanban_tasks a ON a.id = d.task_id\n        JOIN kanban_tasks b ON b.id = d.depends_on_id\n        JOIN kanban_tasks del ON del.id = CASE WHEN a.board = $1 THEN a.id ELSE b.id END\n        JOIN kanban_tasks surv ON surv.id = CASE WHEN a.board = $2 THEN b.id ELSE a.id END\n        WHERE (a.board = $3 OR b.board = $4)\n          AND surv.board IS DISTINCT FROM $5::text\n        ",
  "describe": {
    "columns": [],
    "parameters": {
      "Left": [
        "Text",
        "Text",
        "Text",
        "Text",
        "Text"
      ]
    },
    "nullable": []
  },
  "hash": "47d4c1ca95a669932b56b56f54ffd22f6218433156d9891245537ad432449d51"
}
